name: gridnote
description: Gridnote app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # --- Service Locator ---
  get_it: ^7.7.0

  # --- Estado / DI ---
  flutter_riverpod: ^2.6.1
  provider: ^6.1.5
  collection: ^1.18.0
  crypto: ^3.0.6
  cryptography: ^2.7.0

  # --- UI & Theming ---
  cupertino_icons: ^1.0.8
  flutter_colorpicker: ^1.1.0
  modal_bottom_sheet: ^3.0.0
  wolt_modal_sheet: ^0.6.0
  flutter_easyloading: ^3.0.5
  rounded_loading_button: ^2.1.0
  flutter_spinkit: ^5.2.0
  flutter_speed_dial: ^7.0.0
  flutter_staggered_grid_view: ^0.7.0

  # --- Data Grid & Reporting ---
  pluto_grid: ^8.0.0
  pdf: ^3.11.3
  intl: ^0.19.0
  barcode: ^2.2.7
  csv: ^6.0.0
  syncfusion_flutter_xlsio: any

  # --- Offline First (100% local) ---
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  path: ^1.9.0
  flutter_secure_storage: ^9.2.2

  # --- Notificaciones locales ---
  flutter_local_notifications: ^17.2.1+2
  permission_handler: ^11.3.1
  timezone: ^0.9.3

  # --- Hardware & Plataformas ---
  geolocator: ^10.1.1
  image_picker: ^1.1.2
  connectivity_plus: ^6.1.4
  mobile_scanner: ^7.0.1

  # --- Mapas ---
  flutter_map: ^6.1.0
  latlong2: ^0.9.1

  # --- Sharing & Archivos ---
  share_plus: ^10.1.4
  flutter_email_sender: ^6.0.3
  url_launcher: ^6.3.0
  open_filex: ^4.5.0
  file_picker: ^8.0.3

  # --- IA / OCR / Charts / Importador (opcionales) ---
  google_mlkit_text_recognition: ^0.13.0
  fl_chart: ^0.69.0
  excel: ^4.0.6
  dio: ^5.5.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  very_good_analysis: ^5.1.0
  build_runner: ^2.4.13   # ← corregido para compatibilidad con hive_generator 2.0.1
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
  #   - assets/icons/
  # fonts:
  #   - family: RobotoMono
  #     fonts:
  #       - asset: assets/fonts/RobotoMono/RobotoMono-VariableFont_wght.ttf
